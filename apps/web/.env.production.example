# Production — set in Vercel → Project → Settings → Environment Variables.
# Browser calls same-origin /api; Next rewrites to this host at build time.

NEXT_PUBLIC_API_URL=https://fishmaster-api.onrender.com
# Optional server-only override (share pages / SSR). Defaults to NEXT_PUBLIC_API_URL.
# API_INTERNAL_URL=https://fishmaster-api.onrender.com
